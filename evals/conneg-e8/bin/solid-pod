#!/bin/bash
# Portable Tier-3 shim: TLS via this machine's mkcert CA; CLI path via env or
# the conventional sibling-repo location.
export NODE_EXTRA_CA_CERTS="$(mkcert -CAROOT)/rootCA.pem"
CLI="${SOLID_AGENT_SKILLS:-$HOME/dev/git/LA3D/agents/solid-agent-skills}/dist/cli.js"
exec node "$CLI" "$@"
