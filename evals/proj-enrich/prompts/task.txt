You are working against a Solid Pod at https://pod.vardeman.me/vault/

Do these steps and report what happened:
(1) Read the concept resource https://pod.vardeman.me/vault/wiki/concepts/photosynthesis.md
    and its metadata (the .meta sidecar reached via the describedby Link header).
(2) Add ONE annotation of your own to that concept's metadata: PATCH the .meta resource
    (Content-Type: text/n3, a solid:InsertDeletePatch) inserting ONE triple whose subject
    is the concept's #this entity, using a predicate you mint under
    https://example.org/probe# , recording today's date as an xsd:date literal.
(3) Change the concept's definition sentence in the BODY (keep the prefLabel and everything
    else identical) and PUT the whole body back with Content-Type text/markdown.
(4) GET the .meta again and report: did your annotation survive the body rewrite? Quote the
    relevant metadata triples before and after.

Constraints:
- Interact over HTTP only, using curl. Do not read any other files on this machine,
  do not search the web, and do not use any prior knowledge of this particular Pod or
  project — only generic HTTP / RDF / Linked Data standards and whatever you can learn
  from the Pod itself.

Your final output must contain three sections:
1. ROUTE — at most 10 lines: the path you took and the decisions that drove it
   (not a request log; the harness records every request).
2. ANSWER — did your annotation triple survive the body rewrite (yes/no), with the
   relevant before/after metadata triples quoted, naming which Pod resource each claim
   came from.
3. PROVENANCE — separate what you learned from the Pod itself from what you knew
   from training.
