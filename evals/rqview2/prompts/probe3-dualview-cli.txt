You are working against a Solid Pod at https://pod.vardeman.me/vault/

This Pod stores a memory of concepts. Answer BOTH of these questions about its
memory, using only what the Pod itself contains:

Q1. What is photosynthesis? (a one-sentence explanation)
Q2. Within this Pod's memory, what broader topic is photosynthesis filed under,
    and what *other* concepts are filed under that same broader topic?

Tools available:
- curl
- a command-line tool named `solid-pod` (on your PATH; run `solid-pod --help`
  to find out what it can do — you have not been told anything else about it)

Constraints:
- Use only those two tools. Do not read any other files on this machine, do not
  search the web, and do not use any prior knowledge of this particular Pod or
  project — only generic HTTP / RDF / Linked Data standards and whatever you can
  learn from the Pod itself and the tools. Answer from the Pod's own data, not
  from your background knowledge of biology.
- Keep a trajectory log as you work: for every command you run, record the
  command, the key output, and the reasoning that drove your next step.

Your final output must contain three sections:
1. TRAJECTORY — the full command-by-command log.
2. ANSWERS — Q1 and Q2, each stating which Pod resource(s)/tool result(s) you
   got the answer from.
3. PROVENANCE — separate what you learned from the Pod itself from what you knew
   from training.
